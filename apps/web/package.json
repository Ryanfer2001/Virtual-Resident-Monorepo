{
  "name": "@noszona/web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && node scripts/dereference-node-modules.js",
    "start": "next start",
    "lint": "eslint",
    "postinstall": "node -e \"const fs=require('fs'),path=require('path');function d(dir,seen){if(seen.has(dir))return;seen.add(dir);let e;try{e=fs.readdirSync(dir,{withFileTypes:true})}catch(err){return}for(const x of e){const p=path.join(dir,x.name);if(x.isSymbolicLink()){let r;try{r=fs.realpathSync(p)}catch(err){continue}fs.rmSync(p,{recursive:true,force:true});fs.cpSync(r,p,{recursive:true,dereference:true});d(p,seen)}else if(x.isDirectory()){d(p,seen)}}}d(path.join(process.cwd(),'node_modules'),new Set());console.log('postinstall: dereferenced node_modules symlinks')\""
  },
  "dependencies": {
    "@types/qrcode": "^1.5.6",
    "animejs": "^4.5.0",
    "jsonwebtoken": "^9.0.3",
    "next": "^16.2.11",
    "qrcode": "^1.5.4",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.10",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
